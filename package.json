{
  "name": "bakery",
  "version": "1.0.0",
  "private": true,
  "description": "Dulce Momento — pastelería",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "preview": "npm run preview --prefix web",
    "wireframe:pdf": "node scripts/export-mockup-pdf.js"
  },
  "dependencies": {
    "pdfkit": "^0.20.2",
    "puppeteer": "^25.10.0"
  }
}
